import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        width: 25,
        height: 10,
        fontSize: 15,
        borderColor: '#000'
    }
});

    export default styles()